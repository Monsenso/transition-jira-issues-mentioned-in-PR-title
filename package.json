{
  "name": "transition-jira-issues-mentioned-in-PR-title",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js --minify",
    "watch": "ncc build index.js --watch --minify"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.2",
    "@actions/github": "^2.1.1",
    "node-fetch": "^3.1.1"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.21.1"
  }
}
